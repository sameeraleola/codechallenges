import UIKit

func foldArray(_ arr: [Int], times: Int){
//func foldArray(_ arr: [Int], times: Int) -> [Int] {
    var foldedArray: [Int] = []
    var array = arr
    var index = 0
    
    while !array.isEmpty {
//        foldedArray.append(array.first! + array.last!)
        print(array[index])
        array[index]
        index += 1

    }
    
//    return array
    
}

foldArray([1, 2, 3, 4, 5], times: 1)
//print(foldArray([1, 2, 3, 4, 5], times: 1))
